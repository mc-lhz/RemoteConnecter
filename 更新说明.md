# RemoteConnecter v1.5-beta3 更新说明（相对 v1.5-beta2 增量）

> 本次为 **B站音乐播放稳定性修复**：修复打包后播放报 `[WinError 2] The system cannot find the file specified` 的问题（ffmpeg 落盘路径与代码解析路径不一致）。
> 适用分支：`main`

---

## 一、核心变更（v1.5-beta2 → v1.5-beta3）

### 1. 修复 B站音乐播放 [WinError 2]
- **根因**：[bilimusic_bp.py](functions/bilimusic/bilimusic_bp.py) 用 `resourcePath('bin/ffmpeg.exe')` 解析 ffmpeg（打包后落 `_MEIPASS/bin/ffmpeg.exe`），但 build 脚本 `--add-binary "bin\ffmpeg.exe;."` 把 ffmpeg 落在 `_MEIPASS/ffmpeg.exe`（目标为根目录），路径不匹配 → 回退系统 PATH → 目标机无 ffmpeg → `subprocess.run` 抛 `FileNotFoundError: [WinError 2]` → 前端显示 `播放失败: [WinError 2]`
- **修复**：[build-product.cmd](build-product.cmd)、[build-develop.cmd](build-develop.cmd) 的 `--add-binary` 目标目录由 `.` 改为 `bin`（`bin\ffmpeg.exe;bin`），与 `resourcePath('bin/ffmpeg.exe')` 一致
- **验证**：重新打包后解包确认 `_MEIPASS/bin/ffmpeg.exe` 存在；模拟打包布局实跑 `m4aToWav` 返回 `ok=True`、`wavExists=True`、`duration=7.26`，不再抛 WinError 2

### 2. 版本号
- `v1.5-beta2` → `v1.5-beta3`（[utils.py](utils.py)）

---

## 二、提交记录（v1.5-beta3 区间）

> 待提交推送后补全提交哈希

---

## 三、验证

- 打包产物（`dist/RemoteConnecter.exe`）构建成功
- 解包确认 `_MEIPASS/bin/ffmpeg.exe` 存在，根目录不再有 ffmpeg.exe
- `/bilimusic` 页面 200；m4a→wav 转码链路在打包环境下正常（无 WinError 2）

---

> 以下为 v1.5-beta2 历史说明

# RemoteConnecter v1.5-beta2 更新说明（相对 v1.5-beta1 增量）

> 本次为 **路由风格统一与稳定性修复**：主页直挂根路径、全部蓝图统一无 `url_prefix` 风格、修复 B站音乐 iframe 的 308 重定向、修复打包后蓝图自动扫描注册失败。
> 适用分支：`main`

---

## 一、核心变更（v1.5-beta1 → v1.5-beta2）

### 1. 主页直挂根路径
- 主页由 `/main/` 改为根路径 `/`，删除 [RemoteConnecter.py](RemoteConnecter.py) 中的根路由 308 重定向
- 静态资源路径 `/main/static/*` **保持不变**（改用 `static_url_path='/main/static'` 保留），`index.html` 的 script 引用无需改动
- 前端命令接口同步：`/main/api/command` → `/api/command`（[index.js](functions/main/static/index.js)）

### 2. 蓝图路由风格彻底统一（全部去除 url_prefix）
- `bilimusic`、`update` 跟随 `term`、`screen`、`main` 改为 **无 `url_prefix` 的显式全路径**风格
- [bilimusic_bp.py](functions/bilimusic/bilimusic_bp.py)：`url_prefix='/bilimusic'` 改为 `static_url_path='/bilimusic/static'`；页面路由 `/` → `/bilimusic`
- [update_bp.py](functions/update/update_bp.py)：`url_prefix='/update'` 改为 `static_url_path='/update/static'`；页面路由 `/` → `/update`（接口 `/update/api/update` URL 不变）

### 3. B站音乐 API 路径精简 + 308 重定向修复
- API 由 `/bilimusic/api/bilimusic/{search,control,status}` 精简为 `/bilimusic/api/{search,control,status}`
- 前端 [bilimusic.js](functions/bilimusic/static/bilimusic.js)：`API_BASE` 同步为 `/bilimusic/api`
- 首页路由加 `strict_slashes=False`，**修复首页 iframe 访问 `/bilimusic` 被 Flask 308 永久重定向到 `/bilimusic/` 导致 iframe 空白**的问题（经 cpolar 隧道访问时尤为明显）

### 4. 打包后蓝图自动扫描注册修复（上轮遗留）
- [RemoteConnecter.py](RemoteConnecter.py)：扫描逻辑由 `os.listdir` 改为 `pkgutil.iter_modules`（兼容打包后 PYZ 环境）
- [build-product.cmd](build-product.cmd)：弃用 `--collect-submodules functions`（嵌入版 `python39._pth` 限制 sys.path 导致收集为空），改为显式 `--hidden-import` 逐业务收集
- `functions/` 各业务子包补充 `__init__.py` 转为正规包，确保源码与打包环境均可被 `pkgutil` 枚举

### 5. 版本号
- `v1.5-beta1` → `v1.5-beta2`（[utils.py](utils.py)）

---

## 二、提交记录（v1.5-beta2 区间）

| 提交 | 说明 |
|------|------|
| `d0fee8c` | refactor: 路由风格统一 + 主页直挂根路径, 版本升至 v1.5-beta2 |
| `914bff6` | fix: 打包后蓝图自动扫描注册失败, 改用 pkgutil 扫描并显式收集 functions 子模块 |

---

## 三、验证

- 源码路由表 + Flask test client 全量通过；重新打包 exe 并启动做全量 HTTP 验证：
  - 所有页面路由（`/`、`/terminal`、`/screenshot`、`/bilimusic`、`/update`、`/download`）均 200
  - 所有 API 路由（`/api/command`、`/bilimusic/api/*`、`/update/api/update`、`/download/api`、`/screenshot/api/*`）均 200
  - WebSocket `/terminal/api/ws` 握手 101 成功
  - 所有业务静态资源（`/main/static`、`/term/static`、`/screen/static`、`/bilimusic/static`、`/update/static`、`/shared/static`）均 200
  - 旧路径 `/bilimusic/api/bilimusic/*`、`/main/` 已正确 404

---

> 以下为 v1.5-beta1 历史说明

# RemoteConnecter v1.5-beta1 更新说明

> 本次更新聚焦 **项目架构重构**：业务模块按功能拆分到 `functions/` 目录、蓝图自动扫描注册、路由缩短统一、无用文件清理。
> 适用分支：`refactor-business-structure`（本地）

---

## 一、架构重构（核心变更）

### 1. 业务模块统一收拢到 functions/ 目录
项目根目录原散落 6 个业务蓝图 + 公共静态资源，现全部归入 `functions/`，每个业务独立成包（含自身 templates/static）：

```
functions/
├── main/        # 主页 & 文件管理 (main_bp + file_bp)
├── screen/      # 屏幕截图 / 推流 / 远程控制
├── term/        # 终端 (WebSocket / ConPTY)
├── bilimusic/   # B站音乐 (含 bilimusic.py API 封装)
├── update/      # 自动更新
└── shared/      # 公共静态资源 (jquery 等)
```

### 2. 蓝图自动扫描注册（新增功能无需改主入口）
- [RemoteConnecter.py](RemoteConnecter.py) 移除 6 行硬编码 `import` + 7 行注册代码
- 新增 `discoverAndRegisterBlueprints()`：运行时扫描 `functions/` 下所有 `*_bp.py`，自动收集并注册蓝图与 WebSocket（`sock`）
- **约定**：蓝图模块命名 `*_bp.py`、蓝图变量命名 `*_bp`、WebSocket 变量固定 `sock`
- **以后加新功能**：只需新建 `functions/xxx/xxx_bp.py`，主入口零改动
- 修复 `pkgutil.iter_modules` 无法枚举命名空间包的问题，改用 `os.listdir` 扫描目录

### 3. 路由缩短统一
| 业务 | 原路由 | 新路由 |
|------|--------|--------|
| 终端 | `/term/terminal` | `/terminal` |
| 屏幕 | `/screen/screenshot` | `/screenshot` |
| 主页 | `/main/` | `/`（直挂根路径） |
| B站音乐 | `/bilimusic/` | `/bilimusic`（API 精简为 `/bilimusic/api/*`） |
| 更新 | `/update/` | `/update`（接口 `/update/api/update` 不变） |
| 文件 | 不变 | `/download`、`/upload` |

- 去掉全部蓝图的 `url_prefix`（term/screen/main/bilimusic/update），路由一律写显式全路径，改用 `static_url_path` 保留静态资源路径（`/term/static`、`/screen/static`、`/main/static`、`/bilimusic/static`、`/update/static` 均不变）
- 主界面 iframe、WS URL、控制 API 等引用全部联动更新

### 4. 无用文件清理
- 删除 `templates/design.html`、`templates/control.html`（无路由渲染）
- 删除 `static/input-panel.js`（纯前端测试面板，只弹 alert 无实际功能）
- 删除 `claudecode.cmd`（与项目无关）
- 删除 `ffmpeg-mini.exe`（弃用的 mini 版）
- 清理空目录、`__pycache__` 缓存目录

---

## 二、打包（生产构建）

- 构建脚本 `--add-data` 路径全部加 `functions\` 前缀（源/目标同步）
- 新增 `--collect-submodules functions`：收集 functions 包全部子模块（自动扫描依赖动态导入，PyInstaller 需显式收集）
- 版本号：`v1.4.5-beta1` → `v1.5-beta1`

---

## 三、提交记录（v1.5-beta1 区间）

| 提交 | 说明 |
|------|------|
| `262bf1c` | refactor: 按业务模块重构目录结构，业务独立包 + shared 公共静态 |
| `8501357` | refactor: 构建脚本移回根目录 |
| `f0c7799` | refactor: 业务模块移入 functions/ 目录 |
| `5daea65` | refactor: 蓝图自动扫描注册，新增功能无需改主入口 |
| `205d9b3` | refactor: 终端路由改为 /terminal |
| `0ecc3be` | refactor: 屏幕路由改为 /screenshot |

---

## 四、待办 / 注意事项

- `refactor-business-structure` 分支尚未合并回 `main`，也未推送远程
- 全部蓝图已统一为无 `url_prefix` 的显式全路径风格
- 打包产物验证通过后可考虑合并分支、推送 GitHub/Gitee
