# RemoteConnecter 更新说明

> 本次更新聚焦 **嵌入式终端（WebSocket / ConPTY）** 功能落地、运行稳定性修复、合并回归修复，以及前端交互优化。
> 适用分支：`main`（已推送至 GitHub `origin/main`）。

---

## 一、新增功能

### 1. 嵌入式终端（远程命令行）
- 新增 `/terminal` 终端页面与 `/terminal/api/ws` WebSocket 终端 API。
- 基于 **pywinpty（ConPTY）** 在 Windows 完整还原 cmd / powershell 交互体验（光标、颜色、命令回显、退出码均正常）。
- 主界面底部导航新增「终端」入口（图标已替换为终端 SVG，与导航栏其他图标同尺寸对齐）。
- 支持任意终端程序：cmd.exe / powershell.exe / python / diskpart 等，通过 `?cmd=xxx` 指定。
- 帧协议支持 resize（窗口缩放）、ping/pong 控制帧。

---

## 二、问题修复

### 1. 终端 WebSocket「Invalid frame header」彻底修复
根因多层，已全部解决：
- **pywinpty 跨线程 spawn/read**：原实现在 WSGI 线程创建 pty、在独立线程 read，触发 pywinpty 线程亲和性 bug 立即 EOF。改为 **pty 在 reader 线程内部创建**，spawn/read 同线程。
- **忽略帧误判 EOF**：pywinpty 内部 `b'0011Ignore` 哨兵帧返回空串 `''`，原 `if not data` 误判为进程退出导致连接发完 banner 即关闭。现区分：空 `str` 为忽略帧丢弃，`bytes` 的 `b''` 才是真正 EOF。
- **并发破坏 wsproto 状态机**：reader 线程只入队、主线程统一 `ws.send()`，避免多线程并发发送。
- **write 类型不匹配**：Windows 传 `str`、非 Windows 回退传 `bytes`。

### 2. 合并 term_temp 后功能丢失
合并时 term_temp 的「仅保留终端」改动覆盖了 main 的全部蓝图注册，导致 main/file/screen/update/bilimusic 功能全部丢失。已恢复 6 个蓝图全部注册（main/file/screen/update/bilimusic/term）。

### 3. ModuleNotFoundError: No module named 'Logcat'
根目录缺失 `Logcat.py`（仅 `Installer/Logcat.py` 被 git 跟踪）。已恢复根目录 `Logcat.py`，使 `import Logcat` 可解析。

### 4. 前端 term.html 报错
- `connect is not a function`：预设按钮行内 `onclick` 在全局调用被 IIFE 包裹的局部 `connect`。已**移除 IIFE**，函数置为顶层全局，可直接调用。
- `term.getValue()` 不存在：改为从输入框 `value` 取值。

---

## 三、前端优化

- **term.html 重构**：移除大 IIFE，`var` 全部改为 `let`/`const`，代码更直观。
- **预设按钮样式**：cmd / powershell 按钮改为图标在上、文字在下的上下排列。
- **主界面终端图标**：替换为 Unicode/SVG 终端图标（`bi bi-terminal`），并补齐 `.nav-btn svg` 尺寸规则，与导航栏其他图标对齐。

---

## 四、代码规范

- `term_bp.py` 所有变量、函数名统一为 **小驼峰（lower camelCase）**：`spawnShell` / `_spawnWindows` / `wsTerminal` / `_decodeFrame` / `_handleControl` / `procRef` / `outQueue` / `isCtrl` 等。
- 仅类名（`_PipeProcess`、`Logcat`）使用 PascalCase，常量使用全大写，符合规范。

---

## 五、打包（生产构建）

- `RemoteConnecter.spec` 与 `build-product.cmd` / `build-develop.cmd` 已补充终端依赖的隐藏导入：
  `--hidden-import winpty --hidden-import flask_sock --hidden-import simple_websocket ...`
- 打包命令（窗口模式单文件 + UPX 压缩）：
  ```
  pyinstaller -F -w --upx-dir "upx" --add-data "templates;templates" --add-data "static;static" --add-binary "ffmpeg.exe;." --add-binary "ffprobe.exe;." --hidden-import winpty --hidden-import flask_sock --hidden-import simple_websocket RemoteConnecter.py
  ```
- 注意：打包需在**沙箱外**执行，PyInstaller 会写入系统 `Local\pyinstaller` 缓存目录，沙箱环境会拦截。

---

## 六、提交记录（节选）

| 提交 | 说明 |
|------|------|
| `d9ce38f` | fix: term.html 移除 IIFE 暴露 connect；预设按钮图标文字上下排列 |
| `bc97f90` | fix: 恢复全部蓝图注册，修复合并 term_temp 后功能丢失 |
| `23b1b74` | fix: 恢复根目录 Logcat.py 解决 ModuleNotFoundError |
| `7e39b3b` | Merge branch 'term_temp' |
| `a076223` | style: term_bp 变量与函数名统一为小驼峰 |
| `4b872f6` | fix: 修复终端 WebSocket Invalid frame header 并兼容 pywinpty |

---

## 七、待办 / 注意事项

- `term_temp` 分支上有一批**历史遗留删除改动**仍保存在 stash（`term_temp 遗留删除改动`），未带入 main，也未丢失，需要时可处理。
- 当前 `index.html` 终端图标调整、`RemoteConnecter.spec` 与 build 脚本的隐藏导入补充**尚未提交**，建议在打包验证通过后一并提交。
